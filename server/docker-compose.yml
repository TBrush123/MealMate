version: '3.8'
services:
  db:
    image: postgres:15
    platform: linux/amd64
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Lolkekchebyrek123!
    ports:
      - "5432:5432"